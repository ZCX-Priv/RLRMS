# 计划：重新把「更新状态」section 移到「菜品明细」section 上方

## 摘要

用户反馈订单详情弹窗中「更新状态」没有出现在「菜品明细」上方。经核查，上一轮对模板的顺序调整已被还原（原因不明，可能是手动撤销或 git 操作），但「总计」行的样式放大改动仍在。本计划只需重新应用模板中两个 section 的顺序调整。

## 当前状态分析

目标文件：[src/admin/views/DashboardView.vue](file:///c:\Users\LX\Desktop\RLRMS\src\admin\views\DashboardView.vue)

`.order-detail` 容器（699-756 行）内三个 `.detail-section` 当前顺序：
1. 基本信息（700-722 行）
2. **菜品明细**（724-740 行，`<h4>菜品明细</h4>` 在 725 行）—— 在上
3. **更新状态**（742-755 行，`<h4>更新状态</h4>` 在 743 行）—— 在下

样式区确认：「总计」行放大样式（1131-1145 行，`font-size: 1.125rem`、`font-weight: 700`、金额 `1.25rem` 等）**仍然保留**，无需再改。

排查结论：全管理端仅此一处渲染订单详情弹窗（`title="订单详情"` 仅出现 1 次），不存在其他组件重复渲染的问题。客户端 `OrderDetailView.vue` 用的是 `order-detail-page` class，是独立页面且不含「更新状态」section，与本次无关。

## 拟定改动

### 改动：调整 section 顺序（仅模板，单文件单处）

文件：`src/admin/views/DashboardView.vue`，模板区 724-755 行。

把「更新状态」section（742-755 行）整体移到「菜品明细」section（724-740 行）之前。调整后顺序为：
1. 基本信息
2. **更新状态**（移到此处）
3. **菜品明细**（下移到此）

实现方式：用 Edit 工具交换两个 `<div class="detail-section">...</div>` 块的位置，保持各自内部内容、类名、绑定、事件处理完全不变，仅改顺序。

不改动样式（「总计」行样式已正确保留），不改动逻辑，不新增文件。

## 假设与决策

- **假设**：用户仅要求修复 section 顺序问题，「总计」行样式保持当前（已放大）状态不动。
- **决策**：只重新应用模板顺序调整这一处改动，不重复改动样式，避免过度设计。
- **决策**：不新增任何文件，仅编辑 `DashboardView.vue`。
- **备注**：上一轮改动被还原的原因未查明（可能是手动撤销或 git 操作）；本次重新应用后，建议用户避免再次执行还原操作。

## 验证步骤

1. 运行 `npx vue-tsc -b` 进行类型检查，确认无错误。
2. 在浏览器打开管理后台「概览」页，点击某条订单查看「订单详情」弹窗，确认：
   - 三个 section 顺序为：基本信息 → 更新状态 → 菜品明细。
   - 状态按钮点击仍可正常切换订单状态（功能未受影响）。
   - 「总计」行保持放大醒目效果。
