- **运行时与框架**：Node.js + Express (ESM)。
- **数据存储**：SQLite (via `sql.js`)，以单文件 `restaurant.db` 形式存储。
- **身份认证**：JWT (`jsonwebtoken`) + bcryptjs，支持 Admin 与 Customer 双角色 Cookie 认证。
- **数据校验**：Zod 用于请求体结构化校验。
- **图像处理**：Sharp 用于菜品图片上传处理。