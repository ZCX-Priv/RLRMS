- 所有 API 响应均遵循 `{ success: boolean, data?: any, error?: string }` 的统一 JSON 结构。
- 写操作（INSERT/UPDATE/DELETE）后必须显式调用缓存失效函数（如 `cacheInvalidate`）以保证数据一致性。
- 涉及多步数据库写入的业务逻辑（如下单、重排序）需使用 `beginBatch()` 和 `endBatch()` 包裹以确保原子性。
- 敏感业务接口（如下单、加菜）必须在服务端重新查询数据库价格并计算总额，严禁信任客户端提交的金额字段。