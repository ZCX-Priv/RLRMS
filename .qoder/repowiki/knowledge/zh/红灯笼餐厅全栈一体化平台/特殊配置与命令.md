- **一键开发启动**：`npm run dev` 同时拉起后端 API 服务与前端 Vite 开发服务器。
- **生产环境构建**：`npm run build:production` 自动化完成前端打包与后端编译，生成可部署的 `dist` 与 `server/dist` 目录。
- **容器化部署**：`docker compose up -d` 即可在服务器上启动包含数据库持久化的完整应用实例。