- 所有异步 API 调用均封装在 `src/api/index.ts` 中，返回标准化的 `{ success: boolean; data: T }` 结构。
- 业务逻辑中的可复用状态行为（如轮询、拖拽排序）被提取为 `src/shared/composables` 下的自定义 Hook。
- 视图组件按业务域（admin/client）划分目录，并遵循 `NameView.vue` 的命名规范以区别于通用 UI 组件。
- 认证状态变更通过全局事件总线（如 `auth:expired`）或 Pinia Store 动作触发，确保多标签页或组件间的状态一致性。